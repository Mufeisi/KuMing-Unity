# 迁移铁律（所有任务强制）

## 0. 当前唯一目标

- 每个任务简报必须写明"当前唯一目标"：一个可交付工件
  （一行可跑代码 / 一张 old-new 对比图 / 一份基线数据文件）。
- 任务开始时先复述目标与禁止范围。简报缺这两项时先询问，不得自行扩大。

## 1. 范围门禁

- 只做简报明确写的工作。简报外的问题按以下处理：
  - 与当前目标直接相关 → 做；
  - 无关但重要 → 登记到 `docs/backlog.md`（一行：问题+文件+建议），继续当前任务；
  - 无关且不重要 → 忽略。
- 禁止"顺手"重构相邻模块、补无关测试、修 lint、改进相关代码。
- 禁止在未过 G0（基线可复现构建）之前派任何编写会话。

## 2. 禁止造机器（计划外）

- 禁止为完成任务新建：分析工具、扫描器、Roslyn 分析器、测试框架、
  证据基建、文档体系、矩阵或 GUI。
- 需要"清单/扫描"时，用现有 grep / 现有脚本一次完成，禁止为清单建项目。
- 需要新建项目或工具时，先停下来向用户申请，说明必要性再动手。
- **例外：关键路径上的验证基建**（封包回放器、黄金截图对比工具、图集转换器）
  作为独立显式任务自己占一条简报，不算"计划外"。判断标准：
  该基建是完成迁移所必需、且每个迁移任务都会反复用它，而不是替代真工作的逃难工具。

## 3. 时间预算与升级

- 简报写明预估时间。以下任一信号触发 → 停止，汇报：
  "卡在哪 / 已试什么 / 下一步选项"，等用户裁决：
  - 超过预算 50%（或 2×，取先触达者）；
  - 连续 3 轮无代码改动；
  - 开始造工具；
  - 同一问题 2 次尝试无进展。
- 审核最多两轮。第三轮不通过 → 停止，等用户裁决保留/拆分/放弃。
  禁止进入第四轮。
- 根任务上下文压缩超过 3 次 → 转交新任务，不继续长会话。

## 4. 完成定义 = 工件

- 完成 = 交付了简报指定的工件（commit / 截图 / 数据文件）并通过该任务审核。
- "测试全绿""审核通过"都不算完成；没有产生指定工件的任务视为未完成。
- 每天收尾自问一句："今天的产出是用户能看到的工件，还是过程资产？"
  若是后者，今天不算进度。

## 5. 验证规范

- 简报中的"验证命令"必须是一条**可执行、可复现的命令**，
  跑完直接产出 old/new diff 结果（封包回放状态哈希 / 黄金截图像素差 / 数据 diff），
  禁止用"人工目测"作为验收。
- 行为迁移（封包/状态）：用封包回放 + 状态哈希判定一致，不是截图。
- 视觉迁移：用黄金截图 + 像素差阈值判定，并规定确定性条件
  （同分辨率 / 同资源 / 同基准时间）。
- 无法自动判定的差异必须归类为 Bug / 已接受差异 / 基线错误，禁止"肉眼差不多"关闭。

## 6. 常设不变量

- 每次合并后，旧客户端 + 服务端必须仍可构建、可运行（`dotnet build "Legend of Mir.sln"`）。
- 主会话每次 merge 后跑完整构建，失败即回退或修复，不带着坏构建继续。
- 这条优先于任何功能进度。

## 7. 禁止重造与边界敏感

- 以下模块已有且可用，**禁止重写**：
  渲染接缝（DXManager / SpriteBatchStack）、脚本热更（Server/Scripting）、
  微端（Bootstrap*）、FairyGUI 运行时、协议序列化（Shared/Packet.cs）、
  SchemaMigrator。清单以 `docs/migration-status.md` 为准，会随迁移更新。
- 边界敏感：协议改 Shared 不碰 Share 副本；玩家状态只主线程写；
  脚本只旁路 Hook；渲染走接缝；DB 改表走 SchemaMigration；发布物禁止 Preview。

## 8. 升级层级与定义升级出口

- 升级层级：产品/门禁/定义级问题（验收标准、阶段范围）→ 升级给用户；
  技术实现选择（A/B 方案、摘要模型）→ 自己定并记录，
  **禁止把 1/2/3 类技术选项抛给用户**。
- 定义升级出口：验收标准在当前基建下结构性不可满足
  （如外部资源仅存本机、无 CI → "裸 clone 可重建"无法验证）→ **禁止硬试**，
  先升级定义：拆成可完成项 + 独立后续项。

## 9. 验证成本分层

- 单次验证 > 30 分钟 → 小改动只跑快内环（局部校验），
  慢外环（全量 acquire / 完整发布）只在阶段末或明确的重试验证时跑。
- 每次重跑慢外环前，先书面说明"为什么这次值得跑"。

## 10. 语言纪律（中文保持）

- 本仓库全部交流/文档/状态报告/commit message 一律中文，
  英文仅限代码标识符、命令、报错原文、不可翻译的技术名词。
- **整段英文输出 = 违规**：触发停止条件，立即停下，用中文重发。
- 任务简报、每日工件检查必须含"语言"项。
- 当上下文含大量英文工具输出时，主动用中文转述关键信息，
  减少英文对上下文的污染（防"语言跟随效应"导致输出漂移）。

## 11. GIT 自动推送（阶段完成即推）

- 每个阶段性开发完成（验证通过、已 merge）后，由执行者**自行判断推送时机与方式**，
  直接 `git push`（origin/master 已设 upstream），**不得就"要不要推/怎么推"询问用户**。
- 推送前自检（全过才推）：
  - 常设不变量通过（§6：旧客户端 + 服务端可构建可运行）；
  - 任务验证命令已跑通，工件（截图/数据/脚本）已入库；
  - commit message 为中文（§10），无敏感信息、无大文件误入；
  - 工作区无与本次提交无关的改动混入（工具配置如 reasonix.toml 不入库）。
- 例外：用户明确说"先别推"时停止；推送失败（认证/网络）时诊断修复后重试，
  连续 2 次失败 → 按 §3 汇报，不无限重试。
- 用户裁决权只保留给：force push、跨仓库、公开仓库暴露敏感内容这类不可逆操作。

---

# 会话划分与协作

## 主会话（唯一）

- 职责：任务拆分与简报编写、分配、验收、合并、维护迁移状态、跑常设不变量。
- 每个新会话都是零上下文，简报必须指向权威的
  `docs/migration-status.md`（已迁移清单 / 进行中 / 文件所有权 / 已确立模式）
  与 `docs/three-platform-migration-plan.md`（任务拆分 / 状态 / 排序 / 验收方式）。
  主会话是唯一维护者。

## 编写会话

- 每个任务新建，任务完成后即结束，不回收复用。
- 并行受依赖约束：同一阶段、文件不相交（简报"文件所有权"就是干这个的）才可并行；
  串行阶段老老实实一个编写会话。

## 审核会话

- 审核不必每任务一个 session。多数任务的完成物是可复现的 diff 工件时，
  "跑验证命令"本身就是审核，主会话直接验收（验证命令跑通 + 无越界）。
- 高风险 / 横切任务（渲染核心、协议、GameScene 拆分）才派独立审核会话。
- 大量小工件（inventory 清单、基线快照）批量凑一个审核会话。
- **审核会话受同一条 Scope Gate 约束**：只判断"工件是否达到简报验收标准"，
  禁止开放式提改进意见 / 顺手重构 / 补测试。

## G0 门禁

- 当前仓库 .git 是空壳、Client 缺 obj 无法构建。**第一条任务必须是基线**
  （恢复 git + 固定构建 + 固定快照 + 录旧客户端基线），
  不是"迁 DXManager"。G0 通过前不派任何编写会话。

---

# 任务简报模板（每个任务用这个格式）

```text
任务：<ID>
当前唯一目标：<一个工件，例："DXManager.Draw/DrawOpaque 迁移到 SpriteBatch，
  跑通登录页，产出 1024×768 的 old/new/diff">
禁止范围：<例："不新建工具、不改 GameScene 业务逻辑、不碰音频、不建测试框架">
文件所有权：<列表>
方法约束：<例："边界清单用 grep 一次得出并手工归类；禁止建 Roslyn 分析器">
验证命令：<一条可执行命令，跑完直接出 old/new diff 结果，禁止目测>
验证前置：<若有，指向 docs/migration-status.md 某条目>
预估时间：<人天>
完成工件：<commit + 截图/数据路径 + 验证命令输出>
```

# 好简报 vs 坏简报（真实对比）

**坏（P0-B 实际写法）：** "迁移边界扫描与门禁" → agent 自由发挥，造了 Roslyn 分析器，
打了三轮审核拉锯战。目标模糊给了它"造机器"的空间。

**好（同任务应该怎么写）：**
> 当前唯一目标：产出一份 `docs/migration-file-inventory.md`，
> 用 `grep -rl 'SlimDX|System.Windows.Forms|System.Drawing' Source/Client` 出清单，
> 按 渲染/输入文本/生命周期/音频 四类手工归类，标注每个文件的方法名。
> 禁止范围：不新建项目、不写 Roslyn、不写测试、不建任何工具。
> 方法约束：只用 grep + 文本编辑。
> 验证命令：`wc -l docs/migration-file-inventory.md && head -5 docs/migration-file-inventory.md`
> 预估时间：0.5 天。完成工件：该 md 文件 + 三个 grep 命令的输出。

同样的目标，一半时间，无可争议，无法打转。

# 把重点任务拆成可验证的小步

限制条件能挡住旁支，但挡不住 agent 逃避真正难的工作——因为难的工作没有即时反馈。
把关键路径拆成每一步都产出工件的小任务。

**坏：** "迁移 DXManager"（太大，agent 会先去造工具）

**好：**
1. "DXManager.Draw 单方法迁到 SpriteBatch，登录页背景图 old/new 一致 → 截图"
2. "DXManager.SetBlend 的 Additive 模式 → 火焰特效 old/new 一致 → 截图"
3. "MImage 纹理上传走 Texture2D.SetData，人物站立帧 old/new 一致 → 截图"

每一步 1~2 天，每步一张对比图。agent 在真工作上也能得到"完成"的反馈，
就不会逃向造机器。

# 每日工件检查

每天收尾（由主会话执行）：

- 今天合入了什么可运行的代码？产出了几张 old/new 对比图？
- 如果答案是"建了个工具 / 改了个文档 / 审核又过了一轮"，
  明天把该任务停掉，重写成上面的简报格式再派。
- 判断句："这行代码能被用户看到吗？这张图能证明新旧一致吗？"
  两者都否 → 不是重点，砍。

---

# 输出风格（Token 节约）

- 极简输出：回答只输出核心内容，不写欢迎语/结束语；表格/列表优先于长段落；
  能一行说清的不写两行。**例外：汇报/审核/裁决场景**（§3、审核会话）
  必须写全"卡在哪 / 已试什么 / 下一步选项"，不受极简约束。
- 改代码只给改动部分 + 行号（diff/patch 格式），不重写整个文件；
  首次生成才输出完整代码。
- 描述复杂逻辑用伪代码/结构化格式，不用自然语言长段。
- 大任务拆成小步骤，每步只输出当前步骤的产物。
- 工具输出精简：terminal 命令加 `tail`/`head`/`wc -l` 控制输出量；
  搜索结果只返回文件名+行号；文件读取用 offset+limit，不读整个文件。
